

D3-Anbindung
(simple!) Embedding results in web pages (should remain dynamic)
	export current state as javascript code for its creation

hotkeys sidebar

foreignObject relative width and height
dereference proxies!
cursor to right-left arrow on input field
hide function call interna
	How can a function call be "inlined" into the current context (stepping down into its definition, maintaining context)?
angles in degrees
direct creation of variables
new command: goto, go home, go to point in past, close path
mark all commands in selected scope
show results of expressions beneath input field "=..."

hide duplicate expressions (in loops or recursion)
	simplification of visual steps (with increased number of steps)
selection
	select with rect
	copy and paste selection
	move selection
	z-order-handling
variables can reference variables (on global scope)

allow functions as arguments
Collision-Free positioning

more tooltips
snapping/line end handles

pointer to hand for dragging canvas
How can the program flow be visualised (stepping through)?
	Map a parameter to space (show multiple "shadow instances" of it)
handling endless recursion by checking line-length or out of bounds.
put svg styles in svg css (inkscape compatibility?)
rotate icon arc radius is limited by subsequent line length (no overshoot)
automatic tests (adjust for testability)

round dom numbers


big features
	Branching/Conditions
	constraint solving
	Logo output
	undo
	Beziers
	Bearbeitung von geschlossenen Pfaden (Füllung, ...)

bugs
	fix: rotation manipulation in loop: so that line crosses mouse
	rotate/move/loop with 0 is non-selectable/visible
	svg output does not work (input tag error)
	chrome fix input field text select not visible
